import 'package:flutter/material.dart';

class MessageScr extends StatelessWidget {
  const MessageScr({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('MessageScr'));
  }
}
